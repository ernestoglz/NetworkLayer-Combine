    import XCTest
    @testable import NetworkLayer_Combine

    final class NetworkLayer_CombineTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(NetworkLayer_Combine().text, "Hello, World!")
        }
    }
